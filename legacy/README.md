# Legacy implementations

This directory preserves the Java implementation that preceded the Python
rewrite.

- `java-v1/` is the complete working-tree snapshot at the start of the rewrite,
  including the new mathematical operator work.
- `java-original-release/` is the older archive that was already present in the
  repository.

Neither directory participates in the active Python build.

